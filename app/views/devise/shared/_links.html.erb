

<%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  
  <div class="grupo text-center">
    <%= link_to "Olvidaste la contraseña?", new_password_path(resource_name), class:"color-standar-medio font-weight-bold special-link p-1 show_loader_sup" %>
  </div>
<% end %>

<%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
  <div class=" grupo text-center"><%= link_to "No has recibido las instrucciones de confirmación?", new_confirmation_path(resource_name), class:"color-standar-medio font-weight-bold special-link p-1 show_loader_sup" %></div>
<% end %>

<%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
  <%= link_to "Didn't receive unlock instructions?", new_unlock_path(resource_name) %><br />
<% end %>

<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider) %><br />
  <% end %>
<% end %>
